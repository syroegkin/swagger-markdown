openapi: 3.1.0
info:
  title: API 3.1 Example
  version: "1.0"
paths:
  /ping:
    get:
      summary: Ping
      responses:
        "200":
          description: OK
webhooks:
  orderCreated:
    post:
      summary: Order created
      responses:
        "200":
          description: OK
components:
  schemas:
    Ping:
      type: object
      properties:
        pong:
          type: string
