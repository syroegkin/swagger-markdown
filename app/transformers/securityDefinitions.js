module.exports = securityDefinitions => {
  // Base block
  const res = [];
  // securityDefinitions.map(auth => {
  //
  // });

  // Create header
  // Only in case if there is some data
  if (res.length > 0) {
    res.push('### Security');
    res.push('---');
  }
};
