import { OpenAPIV3 } from 'openapi-types';
// import { Markdown } from '../../lib/markdown';

// Placeholder implementation
// Placeholder implementation
export function transformComponents(_components: OpenAPIV3.ComponentsObject): string | null {
  // const hasComponents = false;
  // const md = Markdown.md();
  // md
  //   .line(md.string('Components').h3())
  //   .line();

  // Object.keys(components).forEach((componentName: string) => {
  //   // const processedDefinition = processDefinition(
  //   //   definitionName,
  //   //   definitions[definitionName],
  //   // );
  //   // if (processedDefinition) {
  //   //   hasComponents = true;
  //   //   md.line(processedDefinition);
  //   // }
  // });
  // if (hasComponents) {
  //   return md.export();
  // }
  return null;
}
